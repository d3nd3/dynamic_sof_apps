---
description: RMF area <input> text input box with options (global, hidden, maxchars, history)
---

### Options
- `global` — any key on screen goes to this box.
- `hidden` — mask input as `*` (e.g., passwords).
- `maxchars <n>` — limit characters; text scrolls/clips.
- `history` — remember typed strings (navigate with arrow keys).

