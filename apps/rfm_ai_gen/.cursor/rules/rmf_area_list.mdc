---
description: RMF area <list list> cycle through items; supports match/bitmask/files
---

### Options
- `atext <text>` — prefix text.
- `match <list>` — parallel values for each item.
- `bitmask <bit>` — map to bit within a cvar value.
- `files <root> <base> <ext>` — generate list from files.

