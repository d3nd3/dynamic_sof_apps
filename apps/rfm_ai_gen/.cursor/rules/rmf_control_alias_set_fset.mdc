---
description: RMF control elements <alias>, <set>, <fset> for console manipulation
---

### alias
Give a simple name to a sequence of console commands.
```text
<alias taunt "say Come get some loser!">
```

### set
Set a console variable immediately when parsed.
```text
<set gl_mode 3>
```

### fset
Set a console variable with a type flag: userinfo(u), serverinfo(s), weapon(w), ammo(a), item(I), misc(m).
Also saved to save games.
```text
<fset knife 6 w>
```

