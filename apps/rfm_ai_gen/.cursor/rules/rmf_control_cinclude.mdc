---
description: RMF control element <cinclude cvar page> include if cvar is non-zero
---

### Purpose
Conditionally include another page when a cvar evaluates to non-zero.

### Example
```text
<cinclude window_avail m_fullscreen_toggle>
```
Includes `m_fullscreen_toggle.rmf` only if `window_avail` is non-zero.

