---
description: RMF control element <key keyname command> to bind a key on a page
---

### Purpose
Bind a specific key on the active page to run a console command.

### Notes
- `keyname` matches bind names (e.g., A, B, ENTER, KP_END) or `any`.
- When pressed, `command` is queued to the exec buffer.

### Example
```text
<key any "popmenu">
<key ENTER "menu options">
```

