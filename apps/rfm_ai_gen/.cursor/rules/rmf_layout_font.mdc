---
description: RMF layout element <font> to set font type and tints
---

### Options
- `type` — `title`, `medium`, or `small`.
- `tint` — default text colour name/value.
- `atint` — highlight text colour name/value.

### Example
```text
<font type medium tint normaltext atint hilitetext>
```

